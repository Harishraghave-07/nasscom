apiVersion: v1
kind: Service
metadata:
  name: phi-service
  namespace: nasscom
spec:
  selector:
    app: phi-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
